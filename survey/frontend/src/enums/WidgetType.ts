/* eslint-disable no-unused-vars */
/**
 * Declares all the possible Widgets
 */
export enum WidgetType {
    CAPTCHA = "captcha",
    CHECKBOX = "checkbox",
    MATRIX = "matrix",
    SELECT = "select",
    TEXTBOX = "textbox",
    TEXTFIELD = "textfield",
    VECTOR = "vector",
}
