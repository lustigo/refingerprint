/**
 * Declares all the possible Widgets
 */
export enum WidgetType {
  MATRIX = "matrix",
  VECTOR = "vector",
  SELECT = "select",
  CHECKBOX = "checkbox",
  TEXTFIELD = "textfield",
  TEXTBOX = "textbox",
  CAPTCHA = "captcha"
}
